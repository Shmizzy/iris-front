---
interface Props {
  totalCreations?: number;
  totalPixels?: number;
  viewers?: number;
}

const { 
  totalCreations = 1855,
  totalPixels = 1546231,
  viewers = 348
} = Astro.props;
---
<div class="stats-container">
  <div class="stats-grid">
    <div class="stat">
      <div class="stat-content">
        <span class="stat-value">{totalCreations.toLocaleString()}</span>
        <span class="stat-label">Total Creations</span>
      </div>
      <div class="stat-icon">🎨</div>
    </div>
    <div class="stat">
      <div class="stat-content">
        <span class="stat-value">{totalPixels.toLocaleString()}</span>
        <span class="stat-label">Total Pixels</span>
      </div>
      <div class="stat-icon">✨</div>
    </div>
    <div class="stat">
      <div class="stat-content">
        <span class="stat-value">{viewers.toLocaleString()}</span>
        <span class="stat-label">Live Viewers</span>
      </div>
      <div class="stat-icon">👁️</div>
    </div>
  </div>
</div>

<style>
  .stats-container {
    background: rgba(20, 20, 40, 0.95);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 1.5rem;
    box-shadow: var(--card-shadow);
    backdrop-filter: blur(10px);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .stat {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 0, 255, 0.1);
    border-radius: 16px;
    padding: 1.25rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: all 0.3s ease;
  }

  .stat:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: var(--accent);
    transform: translateY(-2px);
    box-shadow: 0 0 20px rgba(255, 0, 255, 0.2);
  }

  .stat-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .stat-value {
    color: var(--accent);
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    font-weight: 600;
    font-family: 'Orbitron', sans-serif;
    letter-spacing: 0.5px;
  }

  .stat-label {
    color: var(--text-secondary);
    font-size: clamp(0.75rem, 1.5vw, 0.85rem);
    font-weight: 500;
  }

  .stat-icon {
    font-size: 1.5rem;
    opacity: 0.7;
    transition: all 0.3s ease;
  }

  .stat:hover .stat-icon {
    opacity: 1;
    transform: scale(1.1);
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 0.75rem;
    }

    .stat {
      padding: 1rem;
    }
  }

  @media (max-width: 480px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }

    .stat {
      padding: 1rem;
    }
  }
</style>